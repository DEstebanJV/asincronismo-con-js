// js es un lenguaje asyncronico o no bloqueante

// asincronismo es la accion que no ocurre al mismo tiempo

// memory heap  espacio de memoria compartido

// cola de ejecucion call stack

/* Que son los call stack 

call stack o tambien llamado Pila, Es una estructura simple, similar a un arreglo
en el que solo se puede agregar items al final (push ) y remover el ultimo(pop)


*/

// event loop resolver los call back

/*  Que son los event loop, 


*/

// calbacks promesas y async await

// consumir una  api  y traer unos datos 

// obtener lista completa 

// nombre del primer personaje
